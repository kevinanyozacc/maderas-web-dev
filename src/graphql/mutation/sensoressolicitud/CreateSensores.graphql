mutation CreateSensores($input: [SolicitudSensorInput!]!) {
  createSensores(input: $input) {
    sensores {
      ID
      NUMERO
      DESCRIPCION
      SOLICITUD_ID
      NUME_REGI_SENSOR
    }
  }
}